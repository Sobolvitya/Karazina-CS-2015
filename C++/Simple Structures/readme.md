# Простой пример на классы в C++ 

Класс `Container` описывает интерфейс для некоторого контейнера с методами `push` и `pop`. Класс `Stack` реализует стек с ограничением в 20 элементов, реализующий этот интефейс.    
